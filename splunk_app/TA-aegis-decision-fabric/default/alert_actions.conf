[adf_gate_contain]
is_custom = 1
label = ADF Gate/Prove Contain
description = Decide then gated contain via Aegis Decision Fabric. Default SIMULATE. ML-only DENY. Never equate ML to a signature true positive.
payload_format = json
python.version = python3
param.adf_url = http://127.0.0.1:8080
param.adapter = splunk
param.tool = block_ip
param.mode = simulate
param.prove_token =
